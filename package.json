{
  "name": "relic-run-dungeon-under-rewrite",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "contracts",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server & npm run dev --workspace web",
    "agent:openclaw": "node openclaw/agent-runner-openclaw.js",
    "build": "npm run build --workspace contracts && npm run build --workspace server && npm run build --workspace web",
    "test": "npm run test --workspace contracts",
    "lint": "npm run lint --workspace server && npm run lint --workspace web"
  }
}
